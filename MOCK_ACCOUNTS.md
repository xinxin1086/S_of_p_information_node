# Mock 测试账号说明

本文档列出了系统中可用的测试账号，用于开发和测试。

## 📋 快速开始

系统启动时会自动将以下测试账号初始化到浏览器 localStorage 中，你可以直接使用这些账号登录。

---

## 👥 普通用户账号

**统一密码：`123456`**

| 账号 | 用户名 | 手机号 | 说明 |
|------|--------|--------|------|
| user001 | 张三 | 13800138001 | 普通用户 |
| user002 | 李四 | 13800138002 | 普通用户 |
| wangwu | 王五 | 13800138003 | 普通用户 |
| zhaoliu | 赵六 | 13800138004 | 普通用户 |
| sunqi | 孙七 | 13800138005 | 普通用户 |
| zhouba | 周八 | 13800138006 | 普通用户 |
| wujiu | 吴九 | 13800138007 | 普通用户 |
| zhengshi | 郑十 | 13800138008 | 普通用户 |

---

## 🏢 组织用户账号

**统一密码：`123456`**

| 账号 | 用户名 | 说明 |
|------|--------|------|
| org001 | 环保志愿者协会 | 环保组织 |
| org002 | 钓鱼俱乐部 | 钓鱼爱好组织 |
| org003 | 生态保护基金会 | 生态保护组织 |
| org004 | 自然摄影协会 | 摄影爱好者组织 |

---

## 👑 管理员账号

**统一密码：`admin123`**

| 账号 | 用户名 | User ID | Admin ID | 说明 |
|------|--------|---------|----------|------|
| admin | 系统管理员 | 301 | 1 | 超级管理员，拥有所有权限 |
| admin002 | 内容审核员 | 302 | 2 | 内容审核管理员 |
| admin003 | 用户管理员 | 303 | 3 | 用户管理管理员 |

---

## 🔧 测试账号

**统一密码：`123456`**

| 账号 | 用户名 | ID | 说明 |
|------|--------|-----|------|
| admin | 系统管理员 | 1 | 默认管理员（系统自动创建） |
| test | 测试用户 | 2 | 测试账号 |

---

## 💡 使用说明

### 登录流程

1. 打开系统登录页面
2. 输入上述任一账号和对应的密码
3. 点击"登录"按钮
4. 系统会验证 localStorage 中的用户数据
5. 验证成功后进入系统

### 重新初始化测试账号

如果需要重置测试账号数据，可以在浏览器控制台执行：

```javascript
// 重置所有 Mock 用户数据
window.mockSystem.resetUsers()

// 或者重新加载页面
window.location.reload()
```

### 查看所有用户数据

在浏览器控制台执行：

```javascript
// 查看所有用户统计
window.mockSystem.getUserStats()

// 查看所有用户列表
window.mockSystem.getUsers()
```

---

## ⚠️ 注意事项

1. **开发环境专用**：这些测试账号仅在开发环境（`import.meta.env.DEV = true`）下可用
2. **本地存储**：用户数据存储在浏览器的 localStorage 中，清除浏览器数据会丢失
3. **密码安全**：这些是测试账号，请勿在生产环境中使用
4. **数据持久性**：Mock 用户数据在首次加载时初始化，后续刷新页面不会重复初始化

---

## 🔄 重置数据

如果需要清除所有 Mock 数据并重新初始化：

```javascript
// 清空所有数据
window.mockSystem.clear()

// 重新加载页面
window.location.reload()
```

---

## 📊 数据统计

系统包含以下 Mock 数据：

- **用户总数**: 17 个
  - 普通用户: 8 个 (ID: 101-108)
  - 组织用户: 4 个 (ID: 201, 202, 203, 204)
  - 管理员用户: 3 个 (User ID: 301-303)
  - 测试用户: 2 个 (ID: 1-2)
  - 已删除用户: 2 个（注销记录）

- **Admin 表记录**: 3 条 (Admin ID: 1-3, 分别关联 User ID: 301-303)

- **活动数据**: 8 个活动
- **公告数据**: 10 个公告
- **科普文章**: 12 篇文章
- **论坛数据**: 15 个帖子，30 个楼层，20 个回复

### 重要说明

管理员账号的数据结构：

1. **User 表（user_info）**：包含完整的用户信息
   - `id`: 用户 ID（301-303）
   - `account`: 登录账号
   - `password_hash`: 密码哈希
   - `username`: 用户名称
   - `phone`: 手机号
   - `email`: 邮箱
   - `avatar`: 头像
   - `role`: 用户角色（SUPER_ADMIN/ADMIN）
   - `created_at`, `updated_at`: 时间戳

2. **Admin 表（admin_info）**：管理员附属表（只存储额外状态信息）
   - `id`: 管理员记录 ID（主键，独立自增，如 1, 2, 3）
   - `user_id`: 关联的用户 ID（外键，指向 User 表的 id，如 301, 302, 303）
   - `created_at`: 管理员创建时间
   - `updated_at`: 管理员信息更新时间
   - **注意**：role、account、username 等字段通过 `@property` 装饰器从关联的 User 对象获取

**关键点**：
- `Admin.id ≠ Admin.user_id`
- 管理员的登录凭证（账号、密码）存储在 User 表中
- Admin 表通过 `user_id` 外键关联到 User 表
- 后端通过 `admin.user.role` 获取管理员角色

这种设计允许管理员作为普通用户登录，同时拥有管理员权限。

---

*最后更新时间：2024-03-01*
